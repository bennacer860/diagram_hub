<%= form_with(model: diagram) do |form| %>
  <% if diagram.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(diagram.errors.count, "error") %> prohibited this diagram from being saved:</h2>

      <ul>
        <% diagram.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :source_code %>
    <%= form.text_area :source_code %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
